# Example .env for SQL Server
# Copy to .env and replace values with your DB credentials. Do NOT commit .env to source control.

# Set engine to 'mssql' to enable SQL Server, otherwise 'sqlite' is used
DB_ENGINE=mssql
DB_NAME=your_database_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=127.0.0.1
DB_PORT=1433
# Driver name must match an installed ODBC driver on the host (Windows): e.g. "ODBC Driver 18 for SQL Server"
DB_DRIVER=ODBC Driver 18 for SQL Server
# If your SQL Server uses a self-signed certificate, you may need:
# DB_EXTRA_PARAMS=TrustServerCertificate=yes

# Notes:
# - Install ODBC Driver for SQL Server (Windows): https://learn.microsoft.com/sql/connect/odbc/windows/microsoft-odbc-driver-for-sql-server
# - Install Python package: pip install mssql-django
# - After filling .env, run: python manage.py migrate
